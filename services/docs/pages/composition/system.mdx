### Build

- monorepo를 사용함으로 Nx 빌드 도구를 사용하여 프로젝트 전체를 관리하고 있습니다.
- root / nx.json 및 root/packages.json에서 관리를 하고 있습니다.

`yarn build:all`를 이용해 루트에서 전체 프로젝트를 빌드 할 수 있습니다.
("nx run-many --target=build --all")
